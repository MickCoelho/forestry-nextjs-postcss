---
label: Homepage
hide_body: true
fields:
- name: hero
  type: text
  config:
    required: false
  label: Hero
  hidden: false
- name: sortable_list
  type: list
  config:
    use_select: false
    min: 
    max: 
  label: sortable list
- name: image
  type: file
  config:
    maxSize: 250
  label: image
- name: repeatable_field_group
  type: field_group_list
  fields:
  - name: title
    type: text
    config:
      required: false
    label: title
  - name: text
    type: textarea
    default: ''
    config:
      required: false
      wysiwyg: false
      schema:
        format: markdown
    label: text
  config:
    min: 
    max: 
    labelField: 
  label: repeatable field group
pages:
- content/home.md
