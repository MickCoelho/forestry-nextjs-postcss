---
label: Homepage
hide_body: true
fields:
- name: hero
  type: text
  config:
    required: false
  label: Hero
- name: sortable_list
  type: list
  config:
    use_select: false
    min: 
    max: 
  label: sortable list
- name: image
  type: file
  config:
    maxSize: 250
  label: image
pages:
- content/home.md
